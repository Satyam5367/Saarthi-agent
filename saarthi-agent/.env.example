# Optional. If set, the reasoning agent uses Claude for live call-plan decisions.
# If left unset, the pipeline still runs end-to-end using a deterministic rule-based
# fallback policy — useful for a quick demo without credentials.
ANTHROPIC_API_KEY=
